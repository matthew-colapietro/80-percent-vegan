import { combineReducers } from "redux";

//the result of the reducer data goes to the "", "trash", "event" key
const rootReducer = combineReducers({
  // dairy: DairyReducer,
});

export default rootReducer;